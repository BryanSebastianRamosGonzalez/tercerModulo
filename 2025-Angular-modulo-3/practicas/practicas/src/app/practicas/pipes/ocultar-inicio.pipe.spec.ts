import { OcultarInicioPipe } from './ocultar-inicio.pipe';

describe('OcultarInicioPipe', () => {
  it('create an instance', () => {
    const pipe = new OcultarInicioPipe();
    expect(pipe).toBeTruthy();
  });
});
